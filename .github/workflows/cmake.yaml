name: CMake Multi-Platform Build

on:
  workflow_dispatch:
  pull_request:
    types: [ opened, synchronize, reopened ]
  push:
    tags:
      - "release/*"

jobs:
  build:
    name: "${{ matrix.os }} | ${{ matrix.toolchain }}"
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: write

    strategy:
      fail-fast: false
      matrix:
        include:
          # =====================
          # Linux
          # =====================
          - os: linux
            runner: ubuntu-latest
            triplet: x64-linux
            toolchain: gcc
            cc: gcc
            cxx: g++

          - os: linux
            runner: ubuntu-latest
            triplet: x64-linux
            toolchain: clang
            cc: clang
            cxx: clang++

          # =====================
          # Windows
          # =====================
          - os: windows
            runner: windows-latest
            triplet: x64-windows
            toolchain: msvc
            generator: Visual Studio 17 2022

          - os: windows
            runner: windows-latest
            triplet: x64-windows
            toolchain: clang
            cc: clang
            cxx: clang++

          # =====================
          # Darwin
          # =====================
          - os: darwin
            runner: macos-latest
            triplet: arm64-osx
            toolchain: gcc
            cc: gcc
            cxx: g++

          - os: darwin
            runner: macos-latest
            triplet: arm64-osx
            toolchain: clang
            cc: clang
            cxx: clang++

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Sanitize Version Name
        id: version-name
        if: github.ref_type == 'tag'
        run: |
          ref="${{ github.ref_name }}"
          
          version="${ref##*/}"
          version="${version#v}"
          version=$(echo "$version" | sed 's/[^a-zA-Z0-9._-]/-/g')
          
          if [[ "$version" =~ ^[0-9]+(\.[0-9]+)+([._-][A-Za-z0-9]+)?$ ]]; then
            echo "version=$version" >> "$GITHUB_OUTPUT"
          else
            echo "version=0.0.0" >> "$GITHUB_OUTPUT"
          fi
        shell: bash

      - name: Clone vcpkg
        run: git clone https://github.com/microsoft/vcpkg.git

      - name: Cache vcpkg
        uses: actions/cache@v5
        with:
          path: |
            vcpkg/installed
            vcpkg/downloads
          key: vcpkg-${{ matrix.os }}-${{ matrix.toolchain }}
          restore-keys: |
            vcpkg-${{ matrix.os }}-${{ matrix.toolchain }}-
            vcpkg-${{ matrix.os }}-

      - name: Bootstrap vcpkg
        run: |
          ./vcpkg/bootstrap-vcpkg.sh
        shell: bash
        if: runner.os != 'Windows'

      - name: Bootstrap vcpkg (Windows)
        run: |
          .\vcpkg\bootstrap-vcpkg.bat
        if: runner.os == 'Windows'

      - name: Install dependencies
        run: |
          ./vcpkg/vcpkg install libarchive openssl --triplet ${{ matrix.triplet }}-release
        shell: bash
        if: runner.os != 'Windows'

      - name: Install dependencies (Windows)
        run: |
          .\vcpkg\vcpkg install libarchive openssl --triplet ${{ matrix.triplet }}-release
        if: runner.os == 'Windows'

      - name: Configure cmake
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: >
          cmake
          -S .
          -B build
          -G "${{ matrix.generator || 'Ninja' }}"
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_TOOLCHAIN_FILE=${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake
          -DVCPKG_TARGET_TRIPLET=${{ matrix.triplet }}-release
          -DUNVM_PROJECT_VERSION='${{ steps.version-name.outputs.version }}'

      - name: Build
        run: cmake --build build --config Release --parallel
        shell: bash

      - name: Install
        run: cmake --install build --config Release --prefix build/install
        shell: bash

      - name: Test (help)
        run: |
          ./build/install/bin/unvm
        shell: bash

      - name: Test (list)
        run: |
          ./build/install/bin/unvm list
          ./build/install/bin/unvm list available
          ./build/install/bin/unvm l
          ./build/install/bin/unvm l available
          ./build/install/bin/unvm ls
          ./build/install/bin/unvm la
        shell: bash

      - name: Test (integration, global)
        run: |
          ./build/install/bin/unvm install latest
          ./build/install/bin/unvm use latest
          ./build/install/bin/node --version
          ./build/install/bin/unvm use none
          ./build/install/bin/unvm remove latest
        shell: bash

      - name: Test (integration, local)
        run: |
          ./build/install/bin/unvm install latest
          ./build/install/bin/unvm use latest local
          ./build/install/bin/node --version
          ./build/install/bin/unvm use none local
          ./build/install/bin/unvm remove latest
        shell: bash

      - name: Test (integration, package)
        run: |
          mkdir test
          cd test
          echo '{"name":"test","version":"1.0.0","engines":{"node":">=18 <21"}}' > package.json
          ../build/install/bin/node --version
          rm package.json
          cd ..
          rmdir test
        shell: bash

      - name: Package
        run: |
          mkdir dist
          cd dist
          cpack --config ../build/CPackConfig.cmake -D CPACK_PACKAGE_FILE_NAME=dist

      - name: Upload Packages
        if: github.ref_type == 'tag'
        run: |
          for file in dist/*; do
            [ -f "$file" ] || continue

            ext="$(basename "$file")"
            ext="${ext#*.}"

            target="dist/unvm-${{ steps.version-name.outputs.version }}-${{ matrix.os }}-${{ matrix.toolchain }}.${ext}"

            mv "$file" "$target"

            echo "Upload $target to ${{ github.ref_name }}"
            gh release upload ${{ github.ref_name }} "$target" --clobber
          done
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
